services:
  postgres:
    image: postgres:18
    container_name: game-backend-db
    environment:
      POSTGRES_USER: game_user
      POSTGRES_PASSWORD: game_pass
      POSTGRES_DB: game_backend
    ports:
      - "55432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: